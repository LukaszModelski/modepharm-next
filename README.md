## Generate static paths for pages and categories

- run `node .\scripts\generate-static-paths.js` - It will generate two files. Copy its contents respectively to ` [...page].tsx` and `[category].tsx` files
